#!/bin/bash
# Name: install.sh
# Description: Installer for ubuntu/debian flavour linux distro's.

# Install strings
sudo apt-get install binutils -y -q=2

# Install exiftool
sudo apt-get install exiftool -y -q=2
